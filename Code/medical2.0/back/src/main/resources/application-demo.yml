server:
  port: 7016

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/medical_demo?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8&useSSL=false}
    username: ${DB_USERNAME:demo_user}
    password: ${DB_PASSWORD:demo_password}
jwtUtils:
  id: jwt
  secret: ${JWT_SECRET:your_jwt_secret_here}
  ttlMillis: 36000000
